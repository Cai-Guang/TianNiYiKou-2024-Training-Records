#include <bits/stdc++.h>

using namespace std;

#define int long long

void solve() {
    int n, k;
    std::cin >> n >> k;

    for (int i = 1; i <= n; i++) {
        
    }
}

signed main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);

    int t = 1;
    std::cin >> t;

    while (t --) {
        solve();
    }
}