#include <bits/stdc++.h>

using namespace std;
#define int long long

void tst() {
    std::cerr << std::endl;
}

template<typename T, typename... Ts>
void tst(T t, Ts... ts) {
    std::cerr << t;
    tst(ts...);
}

#define test tst

void solve() {
   
}

signed main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);

    int t = 1;
    std::cin >> t;

    while (t --) {
        solve();
    }
}